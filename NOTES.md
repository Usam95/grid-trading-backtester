# Teaching Notes

- Use numbered recommendations so the learner can answer with a number plus context.
- Explain unfamiliar terms with concrete price, quantity, and lifecycle examples.
- Keep the canonical glossary synchronized with resolved project vocabulary.
- Future glossary additions can be requested as: `Glossary: define “TERM” as “MEANING”; avoid “SYNONYM”.`
- The learner understands basic spot buying and selling but is not yet familiar with futures, options, or margin.
- Mention-only coverage is insufficient: each requested concept needs its behavior, a concrete example or scenario, and a retrieval check. Prefer a short multi-lesson module over compressing many concepts into one page.
- Selected post-only rejection policy: reconcile, refresh book and filters, retry only at a bounded non-marketable price no worse than rung economics, never fall back to taker, and pause/alert when retry limits are exhausted.
