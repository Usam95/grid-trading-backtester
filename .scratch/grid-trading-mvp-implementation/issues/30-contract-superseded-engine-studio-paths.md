# 30 — Contract the superseded engine and Studio paths

**What to build:** Complete the expand–contract migration by removing only the float/duplicate canonical behavior and legacy frontend workflows whose consumers have moved and whose canonical replacements have proven parity. Retain the old repositories as read-only evidence sources rather than production dependencies.

**Blocked by:** 19 — Seal the holdout and paper-candidate package; 27 — Operate Paper and Testnet through Command Canvas.

**Status:** ready-for-agent

- [ ] A dependency/usage inventory proves no canonical consumer still relies on the targeted old semantic or frontend path.
- [ ] Canonical shared scenarios and retained characterization cases pass before and after removal with approved differences explicitly recorded.
- [ ] No duplicate sizing, fill, accounting, lifecycle, reconciliation, risk, or candidate-selection rule remains outside its owning contract.
- [ ] The typed Studio supplies every retained Research and Operations workflow before the migrated vanilla-JavaScript path is removed.
- [ ] Historical legacy outputs remain labelled with their original precision/provenance limitations and are never silently upgraded.
- [ ] `grid-backtest-core`, `grid-backtest-saas`, and `backtester_old` are not imported by the canonical release and remain available as read-only references until separate archival approval.
- [ ] Architecture fitness checks fail if a removed dependency direction or duplicate rule is reintroduced.

